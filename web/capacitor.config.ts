import {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kabeersnetwork.onesyllabus',
  appName: 'One Syllabus',
  webDir: '.',
  bundledWebRuntime: false
};

export default config;
